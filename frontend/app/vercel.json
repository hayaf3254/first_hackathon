{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [{ "src": "app/package.json", "use": "@vercel/static-build" }],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
